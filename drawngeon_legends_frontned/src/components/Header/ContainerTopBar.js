import styled from "styled-components";

export default styled.div`
  width: 100%;
  height: 52px;

  background: var(--backgroundMenu);
  box-shadow: 0px 4px 4px rgba(0, 0, 0, 0.25);
  border-bottom: 1px solid var(--borderMenu);
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 0 18px;
`;
